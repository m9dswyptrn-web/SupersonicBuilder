# Require human OK + Highlights section + longer notes
REQUIRE_OK=1 REQUIRE_HIGHLIGHTS=1 NOTES_MIN_CHARS=160 \
make verify-deep-publish VERSION=5.0.0 OUT=/mnt/data PUSH=github

# Only length check (no Highlights)
REQUIRE_HIGHLIGHTS=0 NOTES_MIN_CHARS=140 \
make verify-deep-publish VERSION=5.0.0 OUT=/mnt/data PUSH=both