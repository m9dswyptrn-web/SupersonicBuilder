# Publish-on-success (diff runs automatically)
cd SonicBuilder\tools
.\verify_deep_publish.ps1 -OutDir "." -Version "5.0.0" -PUSH "github"

# Generate diff report only
.\prepublish_diff.ps1 -OutDir "." -Version "5.0.0"