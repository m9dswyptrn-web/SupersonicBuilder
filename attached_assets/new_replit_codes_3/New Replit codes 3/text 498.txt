document.getElementById('msMute')?.addEventListener('click', ()=>{
  const sw = document.getElementById('optMuteSwitch');
  if (!sw) return;
  sw.checked = !sw.checked;
  handleMuteSwitchChange(sw.checked);
});